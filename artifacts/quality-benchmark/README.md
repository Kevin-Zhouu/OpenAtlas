# OpenAtlas same-prompt benchmark — completed

[Published atlas](http://localhost:8000/notebooks/e2394d6d-e0a0-455c-ac95-f3aab0feb90f)

## Result

- 708 individually selectable meshes, 605,318 triangles, 789 hierarchy nodes and 2,992 layout cells.
- Final standalone HTML: 8,303,957 bytes. SHA-256: `d3a79b943e6565db12e29f49e3cb64b011c696405b8f4f8380021038cdb1483e`.
- All 42 publisher checks passed, including the independent experience review. The reviewer received 15 image-tool results.
- Root systems separate as complete groups. The requested Brain → Cerebrum → Left hemisphere → Frontal lobe drill-down, pagination, source relationships, persistent hiding, Undo, clipping and material modes were verified.
- Normal forward/reverse scrolling uses fractional camera and opacity transitions. Repairs replaced grainy fades, fixed stale labels and first-action Undo, kept the taught optic pathway visible, added guided identification, and corrected phone resizing and sandbox fullscreen.
- Direct-file execution with networking disabled rendered the model and exported PNG without HTTP requests or JavaScript errors. Physical pinch hardware and device-wide performance were not qualified.
- Desktop-agent inspection of the published reader confirmed assembly and complete grouped separation with no console errors.

The final reviewer report is `final-experience-review.json`. Its approval is tied to the artifact fingerprint. This is evidence that the original separation and abrupt-transition defects were resolved; it is not a guarantee that every future generation will match this result or a claim of clinical validation.

## Comparison controls

The implementation brief is byte-identical to original OpenAtlas job `daa1d726-39ee-44ff-a8bb-ffb3c2b5121f`:
`b03990910661c1da22ddeb53f0be9524e9bba43361cc4f3192bde6b86070e0fe`.

GPT-6 Astra, default medium reasoning, manual brief, planning disabled. The fresh application build used licensed geometry references, not a seeded application. No generated application source or manifest was manually edited by the desktop agent to manufacture success; application repairs were made by OpenAtlas agents.

Fresh run `67ff6dfe-ad41-4734-a5fc-0da744efb209` was interrupted during review by usage limits. Normal checkpoint continuations `e3febb8a-213d-4941-8f8f-0baba190e188` and `630a02bd-1094-4419-b838-4c58ce8b53a0` preserved the notebook and implementation. Final version: `86e8317d-4925-4a9b-bb50-32b3069468e0`.

## Harness findings during the benchmark

- Oversized screenshot images reached the model, but their JSON trace could be truncated. Bounded previews now preserve reliable evidence counting; original images remain intact. Two unit tests and a real Docker MCP oversized-image test passed.
- Container output was merged over old host files, resurrecting deleted crash dumps. Output now uses validated snapshots. Invalid archives preserve saved work; deleted source/build files remain deleted. Unit regressions and a real Docker roundtrip passed.
- Codex, MCP Chromium and separate browser tests hit the 256 process/thread ceiling. The limit is now 512; memory and CPU limits remain. Real-container verification passed.
- Independent review identified actual defects after passing mechanical tests and correctly triggered `codex exec resume` repair. Review rejection examples are retained alongside the final pass.

## Fairer, shorter review policy

The user identified repeated broad reviews and late feedback as too slow. The new policy was deployed **after** this benchmark passed, so it did not retroactively approve the artifact. It separates blockers from suggestions, batches initial findings, retains a server-owned checklist through Continue, focuses follow-ups on repairs and affected interactions, and stops quota/tool failures without futile application repair.

Targets: four minutes for initial audit, two for follow-up. Hard ceilings: eight and four minutes. Timing under the new policy has not yet been measured on a live generation. The benchmark's final old-policy review took 638.793 seconds.

Verification: **153 backend tests passed, 13 opt-in skipped**; focused policy tests passed after the final regression-explanation rule. See `REVIEW-POLICY.md` and `rerun-67ff6dfe.json` for details and provenance.
